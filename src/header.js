function Header() {
    this.types = [];

    this.add = function() {

    }

    this.mount = function() {

    }
}

// SUPPORT BOOL:  typedef enum { false, true } bool;

module.export = new Header();